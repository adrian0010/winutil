# winutil

Windows Toolkit — a small GUI helper to install common applications via winget and apply a handful of system tweaks.

Requirements
- Windows 10/11
- PowerShell 5.1+ (run as Administrator for system tweaks)
- winget (for app installation)

Quick usage
- Open PowerShell as Administrator.
- From the repository folder run: `.
winutil.ps1`
